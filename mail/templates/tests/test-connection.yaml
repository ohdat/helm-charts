apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mail.fullname" . }}-test-connection"
  labels:
    {{- include "mail.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mail.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
