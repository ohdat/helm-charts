apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "goods.fullname" . }}-test-connection"
  labels:
    {{- include "goods.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "goods.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
