apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "grpc.fullname" . }}-test-connection"
  labels:
    {{- include "grpc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      grpc: ['wget']
      args: ['{{ include "grpc.fullname" . }}']
  restartPolicy: Never
