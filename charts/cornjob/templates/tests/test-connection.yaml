apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cornjob.fullname" . }}-test-connection"
  labels:
    {{- include "cornjob.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cornjob.fullname" . }}']
  restartPolicy: Never
