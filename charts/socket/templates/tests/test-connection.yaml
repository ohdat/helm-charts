apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "socket.fullname" . }}-test-connection"
  labels:
    {{- include "socket.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "socket.fullname" . }}:{{ .Values.socket.port }}']
  restartPolicy: Never
