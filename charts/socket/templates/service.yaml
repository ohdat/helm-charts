apiVersion: v1
kind: Service
metadata:
  name: {{ include "socket.fullname" . }}
  labels:
    {{- include "socket.labels" . | nindent 4 }}
spec:
  type: {{ .Values.socket.type }}
  ports:
    # - port: {{ .Values.socket.port }}
    #   targetPort: http
    #   protocol: TCP
    #   name: http
    # - port: {{ .Values.socket.grpcport }}
    #   targetPort: grpc
    #   protocol: TCP
    #   name: grpc


  {{- if not .Values.gateway.enabled }}
    - name: http2
      port: {{ .Values.socket.grpcport }}
      protocol: TCP
      targetPort: {{ .Values.socket.port }}
  {{- end }}

  {{- if .Values.gateway.enabled }}
    - name: http
      port: {{ .Values.socket.port }}
      protocol: TCP
      targetPort: {{ .Values.socket.port }}
  {{- end }}
  selector:
    {{- include "socket.selectorLabels" . | nindent 4 }}
