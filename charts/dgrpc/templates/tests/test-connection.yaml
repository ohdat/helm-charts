apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dgrpc.fullname" . }}-test-connection"
  labels:
    {{- include "dgrpc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      dgrpc: ['wget']
      args: ['{{ include "dgrpc.fullname" . }}']
  restartPolicy: Never
