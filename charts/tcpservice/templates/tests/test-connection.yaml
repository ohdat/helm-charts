apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tcpservice.fullname" . }}-test-connection"
  labels:
    {{- include "tcpservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      tcpservice: ['wget']
      args: ['{{ include "tcpservice.fullname" . }}']
  restartPolicy: Never
