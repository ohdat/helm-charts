{{- if .Values.gateway.enabled -}}

apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: {{ include "tcpservice.fullname" . }}
spec:
  selector:
    istio: ingressgateway # use istio default controller
  servers:
    - port:
        number: 800
        name: tcpservice
        protocol: TCP
      hosts: {{ .Values.gateway.hosts }}
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ include "tcpservice.fullname" . }}
spec:
  hosts: {{ .Values.gateway.hosts }}
  gateways:
    - {{ include "tcpservice.fullname" . }}
  tcp:
    - match:
        - port: 800 
      route:
        - destination:
            host: {{ include "tcpservice.fullname" . }} # use tcpservice name 
            port:
              number: {{ .Values.service.port }}      
      corsPolicy:
        allowOrigin:
          - "*"
        allowMethods:
          - POST
          - GET
          - OPTIONS
        allowHeaders:
          - "*"  
        allowCredentials: false        
{{- end }}
